version: '3.4'

services:
  popframe_api:
    image: popframe_api
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "80:80"
      - "9464:9464"
    volumes:
      - ./.env.production:/app/.env.production:ro
